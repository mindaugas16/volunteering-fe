import { ModalHeaderDirective } from './modal-header.directive';

describe('ModalHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
