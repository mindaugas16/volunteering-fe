import { InputLabelDirective } from './input-label.directive';

describe('InputLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLabelDirective();
    expect(directive).toBeTruthy();
  });
});
