import { InputButtonDirective } from './input-button.directive';

describe('InputButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new InputButtonDirective();
    expect(directive).toBeTruthy();
  });
});
